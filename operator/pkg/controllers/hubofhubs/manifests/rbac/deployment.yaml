---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hub-of-hubs-rbac
  namespace: open-cluster-management
  labels:
    name: hub-of-hubs-rbac
spec:
  replicas: 1
  selector:
    matchLabels:
      name: hub-of-hubs-rbac
  template:
    metadata:
      labels:
        name: hub-of-hubs-rbac
    spec:
      serviceAccountName: hub-of-hubs-rbac
      containers:
        - name: hub-of-hubs-rbac
          image: {{.Image}}
          imagePullPolicy: Always
          args:
          - "/data/opa_authorization.rego"
          - "/data/data.json"
          - "/data/role_bindings.yaml"
          - "--tls-cert-file=/certs/tls.crt"
          - "--tls-private-key-file=/certs/tls.key"
          - "--authorization=basic"
          volumeMounts:
           - readOnly: true
             mountPath: /data
             name: opa-data
           - readOnly: true
             mountPath: /certs
             name: certs
      volumes:
       - name: opa-data
         secret:
           secretName: opa-data
       - name: certs
         secret:
           secretName: hub-of-hubs-rbac-certs
