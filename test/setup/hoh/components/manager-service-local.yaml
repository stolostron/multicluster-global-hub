# use the nodeport service for test
apiVersion: v1
kind: Service
metadata:
  name: hub-of-hubs-manager-nonk8s-service
  labels:
    name: hub-of-hubs-manager
    service: hub-of-hubs-manager
    test: e2e
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: hub-of-hubs-manager-certs
spec:
  type: NodePort
  ports:
    - nodePort: 30080
      port: 8080
      name: http
      protocol: TCP
  selector:
    name: hub-of-hubs-manager
