apiVersion: v1
kind: ConfigMap
metadata:
  name: multicluster-globalhub-config
  namespace: hoh-system
  labels:
    name: multicluster-globalhub-config
data:
  aggregationLevel: "full"
  enableLocalPolicies: "true"
